<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Exclude all files - we don't want automatic backups due to encryption keys -->
    <exclude domain="sharedpref" path="." />
    <exclude domain="database" path="." />
    <exclude domain="file" path="." />
</full-backup-content>
